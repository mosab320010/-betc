apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eduverse-ingress
spec:
  rules:
    - host: eduverse.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 5173
